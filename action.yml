name: 'Preview Delete Action'
description: 'Action template for preview request'
author: 'zezaeoh <zezaeoh@github.com>'
inputs:
  application:
    required: true
    description: 'application id (name)'
  branch:
    required: true
    description: 'branch name'
  release-name-length:
    required: false
    description: 'release name length'
runs:
  using: 'node12'
  main: 'dist/index.js'
